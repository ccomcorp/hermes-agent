# 2026-07-27 Handoff — safe Desktop shutdown and resume

## Safe to close

The current Desktop session may now be closed. No source edits, tests, or background servers are still running from this work. In-process delegations and Desktop-owned backend processes are expected to terminate when the app closes; this handoff and the matching per-project checkpoint preserve the restart point.

## Resume sequence

1. Relaunch Hermes Desktop and select the active **Hermes Agent** project, whose primary workspace is `D:\HeicH\hermes-agent`.
2. Start by reading this handoff, then verify source state rather than trusting any session summary:

   ```bash
   git status --short --branch
   git log --oneline -6
   .venv/Scripts/hermes-real.exe dox status --root D:/HeicH/hermes-agent
   ```

3. The only expected dirty paths are generated Desktop package directories:

   ```text
   apps/desktop/release-next/
   apps/desktop/release-next2/
   ```

   They are deliberately untracked and must never be committed. They were locked by active `Hermes.exe` instances during this session. After all Desktop instances exit, remove them with:

   ```bash
   rm -rf apps/desktop/release-next apps/desktop/release-next2
   git status --short
   ```

   Do not use `git clean` broadly.

4. Restarted Desktop/backend processes load the committed source changes. Confirm the Spawn tree with a small two-child `delegate_task` batch; it should show each child's task and model rather than one generic row. If it does not, capture the `async_delegation` event payload before making another code change.

## Committed and verified work

- `e346faa5c fix(delegation): restore batch progress contract`
  - Restores `progress_fn`, progress-based stale handling, and complete batch events carrying child task/model/session identity for the Desktop Spawn tree.
  - Gates: async-delegation, gateway delegation, and TUI child-mirror tests passed; Desktop `subagents` store tests passed.

- `e8ec818c7 fix(compression): ignore timeline-only model switches`
  - Prevents display-only model-switch timeline markers from aborting context rotation and removes a false cumulative worker-compaction block.
  - Gates: focused compression/state suite passed.

- `ba04bf3a7 test(composite): use native package imports`
  - Removes obsolete external AIOS path injection from composite tests.
  - Gates: composite focused suites passed.

- Project registry repair was completed before this handoff:
  - `hermes-agent` is the active project and resolves to `D:\HeicH\hermes-agent`.
  - The legacy duplicate project record was archived, not deleted.
  - The registry database was backed up before mutation.

## Work deliberately still open

1. **Kanban compression exhaustion:** the harmful cumulative auto-block is removed, but a replacement worker-only terminal-overflow signal has not been designed or implemented. Resume at the real `conversation_loop` terminal-overflow boundary; do not reintroduce callback-based counting of successful compactions.
2. **Live Spawn-tree verification:** source and focused tests prove the restored event contract, but the restarted packaged Desktop UI still requires a live two-child verification.
3. **Safe landing:** after the Desktop process releases generated package files, remove only the two `release-next*` directories above and verify the tree again.
4. **Gate 0 / B4:** parked; do not resume it until the compression and landing work above are explicitly triaged.

## Guardrails for the next agent

- Do not commit generated `.asar` or `release-next*` artifacts.
- Keep composite-memory changes in `D:\HeicH\hermes-agent`; do not move them into the Obsidian project.
- Do not blindly merge `upstream/main`; the branch remains a fork with local commits and needs a throwaway-worktree merge rehearsal after the tree is clean.
- Do not claim the Desktop Spawn tree is fixed live until after the restart and the two-child probe.
- Run `scripts/run_tests.sh` rather than direct `pytest` for Python verification.
