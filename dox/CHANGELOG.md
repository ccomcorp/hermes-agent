# Changelog — hermes-agent DocOps ledger

Keep entries short. Shard or archive if approaching the ledger word budget in `docops.yml`.

## Unreleased

### Tooling / guardrails

- **2026-07-23** — Upstream/main merge into the customized Hermes chassis on `merge/upstream-main-test-20260723-005006`. Preserved AIOS/local seams while accepting upstream refactors: `agent.turn_context` prologue extraction plus a compatibility `conversation_loop.inject_turn_context` seam for consumed-at-injection tests; `api_content` + `spoken_reply` persistence paths; DOX advisory injection; Honcho declared-config Windows HOME scoping; desktop `spoken_reply`/voice tests, project/sidebar/settings merge, and PaneShell compatibility for the local Workbench. Fixed merge-induced gates: Vitest v4 test-context cleanup in `electron/hardening.test.ts`, headless-SPA env isolation in web-server tests, and Honcho global config path resolution. Gates: Python compileall; desktop typecheck; desktop build/assert-dist; web server suite **468 passed / 24 skipped**; memory/Honcho suite **258 passed**; doctor/provider/parity seam suite **177 passed**; desktop focused UI/electron suites **63 passed**; injection seam **2 passed / 10 skipped**. Local merge commit created; adversarial review pending before remote push/promote.
- **2026-07-23** — Adversarial merge review follow-up: fixed reviewer B1 in `/api/ops/backup`, where the explicit dashboard backup output path was appended twice as `-o <path> -o <path>` after the upstream merge kept both blocks. Simplified the branch to append a single explicit `-o` and added `test_ops_backup_explicit_output_passes_single_output_flag`. Gates: `py_compile` for `web_server.py` + test file; focused backup endpoint tests **4 passed**; `test_web_server.py` in clean env **469 passed / 24 skipped**; loop liveness with AIOS packages+health **PASS**; composite memory suite **112 passed / 1 skipped**; DOX advisory + injection smoke **2 passed / 6 skipped**; desktop typecheck and build/assert-dist green. Push/promote still gated on final approval.
- **2026-07-22** — Kanban worker-lifecycle Windows portability (`hermes_cli/kanban_db.py`, commit `8b53cd5b7`). Three pre-existing uncommitted platform fixes in the working tree, understood + corrected before committing (no DOX provenance existed for them). **(1)** `_classify_worker_exit`: `os.WIFEXITED/WEXITSTATUS` raise on Windows → recorded raw exit statuses degraded to `("unknown", None)`, losing the rate-limit sentinel (exit 75, must requeue without counting a failure). Added a raw-form decoder (`0`, `75`, `75<<8`, high-byte-encoded) after the POSIX branch; unreachable on POSIX. **(2)** `_terminate_reclaimed_worker`: `os.kill` on a missing PID raises `ProcessLookupError` on POSIX but generic `OSError` on Windows, so the "already-gone worker == terminated" logic (`35e7ca03d`) never fired on Windows — the reclaim-defer guard read a dead worker as "survived" and deferred forever, stranding stale tasks. **Root cause + fix:** the original uncommitted attempt used a blunt pre-`kill` early-return (`if not _pid_alive: terminated=True; return`) which regressed `test_stale_claim_reclaimed` by skipping the defensive SIGTERM the reclaim contract requires (`06f24351c`). Replaced it with a targeted fix in the `OSError` branch that consults the cross-platform `_pid_alive` probe and sets `terminated=True` only when the worker is genuinely gone — satisfying BOTH the always-SIGTERM contract and the Windows dead-PID detection (`test_dispatch_once_integrates_stale_detection`). **(3)** `list_profiles_on_disk`: fall back to `~/.hermes` when `HERMES_HOME` unset. Test tweak: `Path.as_posix()` on `co_filename` in the dispatcher time-travel mock (slash-agnostic compare). Gates: kanban_db + kanban_core suites **387 passed / 1 skipped**; A/B proof — clean tree 17 fail → uncommitted-as-is 12 fail (1 a self-inflicted regression) → this fix 11 fail, **zero net-new vs clean tree**. Remaining 11 are unrelated pre-existing Windows gaps (git-worktree materialization, `resolve_hermes_argv` PATH s... [truncated]
- **2026-07-21** — Route Advisor B1 lane-by-type + verification nudge: `route_advisor` remains cache-safe/nudge-only but no longer hardcodes `route="coding"`. Added explicit intent detectors for debugging/frontend/research/architecture in `plugins/route_advisor/signals.py`; `build_nudge` now selects only configured lanes (`debugging`, `frontend`, `research`, `planning`/`thinking`, fallback `coding`) and keeps `lane_by_type:false` as legacy coding-only behavior. Added opt-in `verify_nudge` (bundled default false) that detects recent `delegate_task` calls from history and nudges independent validation through `critic`/`source-checker`/`review`. Runtime config in `D:/HeicH/hermes-home/config.yaml` opted in: `mode:nudge`, `min_level:moderate`, `lane_by_type:true`, `verify_nudge:true`. Gates: `tests/plugins/test_route_advisor.py` 12/12 green; config validation/drift 22/22 green with `tests/hermes_cli/test_config.py` still at 2 unrelated pre-existing/default-drift failures (AppData home path, voice spoken_max_chars). Live probe confirmed debugging→`debugging`, frontend→`frontend`, research→`research`, architecture→`planning`, verify→`critic`. Restart required for running gateway sessions to load the plugin/config changes.
- **2026-07-21** — Cron-flag leak into interactive approvals: the in-process scheduler (`gateway/run.py` `InProcessCronScheduler`) set process-global `os.environ["HERMES_CRON_SESSION"]="1"` in `cron/scheduler.py::run_job` and never cleared it, so after any cron tick the co-located gateway process blocked interactive `execute_code` (and dangerous commands) via `check_execute_code_guard`'s `cron_mode: deny` branch (`tools/approval.py:3121`). Fix: cron-ness is now a thread/task-local `contextvars.ContextVar` (`_hermes_cron_ctx` + `set/reset_hermes_cron_context` + `_is_cron_session()`, ctx-first with env fallback), mirroring the `_hermes_interactive_ctx` migration that fixed the same `os.environ` race (GHSA-96vc-wcxf-jjff). 4 readers in `tools/approval.py` converted; `run_job` binds a token and resets it in `finally` (atop the `copy_context()` boundary that already discards it). Regression: `TestCronContextIsolation` (3 tests incl. cross-thread non-leak + execute_code-unaffected-by-other-thread's-cron) in `tests/tools/test_cron_approval_mode.py`. Gates: `scripts/run_tests.sh` cron-approval + execute_code cluster 57/57 green; `test_approval.py` steady at 2 pre-existing `TestDetectDangerousRm` failures (unrelated, proven identical on pristine tree). Rejected `cron_mode: approve` (masks the leak by disabling the guard for genuine cron runs). Restart of desktop/gateway required to flush the poisoned env in the running process.
- **2026-07-21** — Tool-loop guardrail false-positive fix: truthful negatives (process `not_found`, read_file `File not found`, generic absence phrasing) no longer feed the args-blind same-tool failure counter, so diagnostics against absent state can't march to `same_tool_failure_halt`. Exact-signature counter stays broad (identical-args repetition of any failure type still blocks); genuine execution failures still warn/halt at configured thresholds; a truthful negative neither increments nor resets a genuine streak. New predicate `tool_result_is_truthful_negative` in `agent/tool_result_classification.py`; counter wiring in `agent/tool_guardrails.py::after_call`. Regression tests: 4 new in `tests/agent/test_tool_guardrails.py`. Gates: guardrails + runtime + classification 30/30 green. Context: halts observed during legitimate diagnostics against a crashed process registry; operational root cause (two concurrent Hermes instances sharing one HERMES_HOME) documented in session-log.

### Desktop / voice

- **2026-07-22** — Slash-command popover mid-message: `detectTrigger` in `apps/desktop/src/app/chat/composer/text-utils.ts` anchored the `/` trigger strictly at position 0, so the command popover only opened when the draft *started* with `/` — combining commands mid-message gave no completion UI. Now scans for the LAST `/` that starts a token (start-of-text or after whitespace) and matches the command grammar from there; file paths (`src/foo/bar`), ratios (`1/2`), URLs, and `hello / world` still don't trigger; with multiple slash tokens the popover tracks the one being typed. Tests: `text-utils.test.ts` 14/14 green (mid-message cases flipped from `toBeNull` to positive expectations + new last-token and non-token guards). tsc/eslint clean on touched files; other composer test-file failures are the pre-existing `document is not defined` jsdom environment issue (proven identical on stashed tree). Commit `e3d42c85e`. Desktop rebuild required to ship.
- **2026-07-16** — P2a Voice conversation hardening: fixed cancel-during-speak race in `use-voice-conversation.ts` (`cancelledRef` guards re-arm after `end()`). Added 14 Vitest unit tests across `use-voice-conversation.test.tsx` (4 race-condition tests: cancel-end during speak, status transitions, re-arm after cancel) and `use-auto-speak-replies.test.tsx` (10 tests: spoken_reply fallback, hold-until-idle, conversation-inactive guard, empty/null reply, dedupe). EVAL-VDP-006 (package audit): confirmed zero HuggingFace/transformers/S2S dependencies in desktop.

### Operator / layout

- **2026-07-16** — Chassis relocation investigation: GitHub origin already independent (`ccomcorp/hermes-agent`). Prefer clone/move existing fork to NEW_PATH + rewire path pins (`launch-dev-hermes`, update-guard, kanban workdir, `AIOS_PACKAGES_DIR` for composite). Do **not** create a second GitHub repo. Plan: `H:/WSpace-Hermes/hermes-projects/docs/hermes-agent/plans/chassis-relocation-investigation-2026-07-16.md`. Cutover pending user NEW_PATH + WIP commit.

### DocOps

- **2026-07-16** — `hermes dox init --mode hybrid` on chassis root. Layers: contract + ledger + publish. Markers: `docops.yml`, `dox/`, LIVE-STATUS, `docs/standards/DOCUMENT-MANAGEMENT.md`, `<!-- hermes-dox -->` on `AGENTS.md`. **Rule:** all development/changes must maintain DocOps (contract walk, ledger, LIVE-STATUS when status moves).
- **2026-07-16** — Desktop DocOps panel: wrap in `Panel`/`OverlayView` (stop full-window takeover), switch to theme tokens/`PanelPill` (Editorial/Slate-safe), restore dismiss (Esc/X/backdrop) + Refresh/Run Check. Files: `apps/desktop/src/app/docops/index.tsx`, `index.test.tsx`. Gate: typecheck + 18 docops UI tests.
- **2026-07-17** — DocOps target resolution fix: panel opened to wrong/uninitialized location when a project was selected. Backend `status_project`/`check_project` gained `search_parents` (walk up to nearest `docops.yml`); `/api/dox/status` + `/api/dox/check` pass it so a subfolder cwd (or a Windows junction like `Azure-Infrastructure` → `Microsoft-O365-Azure/plans/azure-alpha-remediation`) resolves to the real initialized root. Status payload returns resolved `root`. Desktop `DocOpsView` targets active project's primary workspace path (fallback: live cwd) instead of raw `$currentCwd`. Files: `agent/dox/core.py`, `hermes_cli/web_server.py`, `apps/desktop/src/app/docops/index.tsx`, `apps/desktop/src/types/hermes.ts`, `tests/dox/test_dox_core.py`, `apps/desktop/src/app/docops/index.test.tsx`. Gates: 29 dox py green, 20 docops UI tests green, backend edits lint-clean. (Same change first landed in the `I:` clone as `60009591a`; ported here to the live `D:` chassis.)

### Core product rules (docs)

- **2026-07-16** — Self-improvement loop recorded as **core functionality** in `AGENTS.md` (What Hermes Is + dedicated section). Recursive learning circulation: author → recall → outcome reward. Enhance existing composite/background_review/outcome path only — no parallel mass-reward daemon.

### Memory / learning chassis (working tree)

- **2026-07-16** — Desktop/gateway outcome-signal fix: task-local `push_active_agent` in `AIAgent._execute_tool_calls`; engineering_loop `_emit_outcome_signal` uses it; active harness auto-signals test-like terminal exit codes. Verified: composite tests 112 passed + 1 skipped.

### Operator home (hermes-home, not this git tree)

- MEMORY/USER consolidated; QMD MCP re-enabled; gbrain skill narrative reconciled; dream cycle script import+`--dir` (see hermes-home cron `dbb156f78f80`).
- **2026-07-17** — cli: fix spurious `Unknown toolsets: mcp-<server>` init warning — pre-discovery validation now accepts the canonical `mcp-<server>` toolset form for configured `mcp_servers` (`_unknown_toolsets_pre_discovery`, cli.py; 7 regression tests in tests/hermes_cli/test_toolset_init_warning.py).
- **2026-07-20** — Delegation routes skills update: `hermes-model-routing` v1.1.0 (Named delegation routes — config shape, route param usage, precedence chain, credential-hygiene rule, effect timing table, live-probe recipe, routing policy + orchestration guidance, Route Advisor plugin). `hermes-team-routing` v1.0.1 (Delegation routes cross-reference section — profile vs route decision matrix, when NOT to use routes). Kanban HS-4 (t_a6ecfe10) closeout.
